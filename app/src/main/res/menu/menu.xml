<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/filter_menu"
        android:icon="@drawable/filter_menu_icon"
        android:title="filter"
        app:showAsAction="always" />

    <item
    android:id="@+id/search_menu"
    android:icon="@drawable/ic_search_white_24dp"
    android:title="@string/search_menu_text"
    app:showAsAction="always"/>

    <item
    android:id="@+id/hamburger_menu_main"
    android:icon="@drawable/ic_more_vert_black_24dp"
    android:title="Menu"
    android:popupBackground="@android:color/white"
    android:popupMenuStyle="@style/ThemeOverlay.AppCompat.Light"
    android:background="@null"
    app:showAsAction="always">
    <menu>
        <item android:id="@+id/profile_sub_menu" android:title="Мой Профиль"></item>
        <item android:id="@+id/settingsSubMenu" android:title="Настройки"></item>
    </menu>

    </item>

</menu>

